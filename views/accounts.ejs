<h1><%= title %></h1>
<ul>
    <% for(var i=0; i<accounts.length; i++) { %>
        <li>
            <a href='accounts/<%= accounts[i].id %>'>
                <%= accounts[i].name %>
            </a>
        </li>
    <% } %>
</ul>